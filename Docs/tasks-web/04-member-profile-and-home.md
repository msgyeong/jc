# 4단계: 회원/프로필 및 홈 화면 기능 구현

> **체크리스트·완료 기준**: **../tasks-common/04-member-profile-and-home.md**와 동일합니다.

---

## 웹 구현 참고

- **홈**: `web/js/home.js` — 배너, 공지 요약, 일정 요약, `loadHomeData()`.
- **회원**: `web/js/members.js` — `apiClient.getMembers()`, `searchMembers()`. API: `api/routes/members.js`.
- **프로필**: `web/js/profile.js` — `apiClient.getProfile()`, `updateProfile()`. API: `api/routes/profile.js`.

완료 시 **tasks-common/04** 체크박스 동기화.
