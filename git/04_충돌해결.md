# 🟢 충돌 해결 & 머지 전 체크 (minsu 브랜치 기준)

아래 내용을 그대로 실행해줘.

1. 이 PR(`minsu` → `main`)이 머지될 때 충돌 가능성 있는 파일이 있는지 확인해줘.
   - `git fetch origin` 후 `git diff origin/main...minsu --name-only` 로 확인

2. 충돌 가능성이 있는 파일이 있으면:
   - 어떤 파일인지 목록으로 알려줘.

3. 로컬에서 충돌을 미리 해결하는 절차를 단계별로 진행해줘:
   - Step 1: `git fetch origin`
   - Step 2: `git checkout minsu`
   - Step 3: `git rebase origin/main` (또는 `git merge origin/main`)
   - Step 4: 충돌 파일 열어서 확인 → 수정
   - Step 5: `git add .` → `git rebase --continue` (또는 `git commit`)
   - Step 6: `git push origin minsu` (강제 push 필요 시 `--force-with-lease`)

4. 충돌 해결 완료 후 PR을 다시 확인하는 방법을 알려줘.
